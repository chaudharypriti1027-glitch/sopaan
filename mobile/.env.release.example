# EAS project ID from `eas init` / Expo dashboard
# Required for OTA updates in production builds
EAS_PROJECT_ID=

# Baked into release builds — set per profile in eas.json
# EXPO_PUBLIC_API_URL=https://api.sopaan.app
