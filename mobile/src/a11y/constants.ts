/** WCAG-aligned minimum interactive target (Apple HIG / Material). */
export const MIN_TOUCH_TARGET = 44;

/** Cap dynamic type scaling so layouts remain usable. */
export const MAX_FONT_SIZE_MULTIPLIER = 1.5;

/** Slightly tighter cap for dense UI (tabs, chips). */
export const MAX_FONT_SIZE_MULTIPLIER_DENSE = 1.35;
