import { PREMIUM } from '../premium/premiumStyles';

/** Shared tokens for stack / feature screens (social, learning, prep). */
export const APP_UI = {
  bg: PREMIUM.bg,
  ink: PREMIUM.ink,
  navy: PREMIUM.accent,
  gold: PREMIUM.gold,
  goldSoft: PREMIUM.goldSoft,
  sage: PREMIUM.sage,
  sageSoft: PREMIUM.sageSoft,
  sectionLabel: PREMIUM.sectionLabel,
  cardRadius: PREMIUM.cardRadius,
  hairline: PREMIUM.hairline,
} as const;
