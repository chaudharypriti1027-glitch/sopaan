/**
 * Optional premium neural TTS (ElevenLabs / Google Cloud TTS) can be added later as:
 * POST /api/tts → { audioUrl } played via expo-av, gated behind Pro.
 * expo-speech remains the free offline default.
 */

export {};
