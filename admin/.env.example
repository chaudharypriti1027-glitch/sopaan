# Optional — API origin when admin and API are on different hosts.
# Leave unset for local dev (Vite proxies /api to localhost:4000)
# and production (admin served from the API at /admin/).
# VITE_API_BASE=http://13.220.184.130:4000
